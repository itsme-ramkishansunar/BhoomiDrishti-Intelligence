# U71.4 — Intake & Import Hardening

- Hardened multipart upload parsing for Project Intelligence Intake so browser FormData file parts are located reliably instead of assuming the first multipart part is the file.
- Added project import field aliases for SIH-style names such as `project_name`, `project_code`, `total_land_parcels`, `pending_compensation_cases`, `project_latitude`, and `project_longitude`.
- Added client-side India GIS coordinate validation for imported project rows with clear row-level errors.
- Extended the downloadable CSV template with optional latitude/longitude columns and a clearly labelled demonstration row.
- Added intake/import contract checks to `preflight:local`.
- No persistent database reset or deletion.
