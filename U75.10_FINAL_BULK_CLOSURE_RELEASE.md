# BHOOMIDHRISHTI U75.10 — FINAL BULK CLOSURE

Version: `1.0.30-u75.10-bulk-closure`

## Objective

Close the bulk-development loop without replacing the U74/U75 architecture:

`upload → forensic profile → validation → matching → conflict review → safe promotion → bootstrap → unified snapshot → feature registry → risk → early warning → action intelligence → timeline → intervention intelligence → GIS/dashboard`

U75.10 adds the final operational closure layer:

- bulk health summary
- row-level preflight before promotion
- ingestion-run reconciliation
- batch/evidence/project accounting checks
- server-side downstream propagation after safe promotion
- final release smoke gate

## Safety

- No production SQLite database is bundled.
- No destructive migration is introduced.
- Existing U75.8 safe promotion remains authoritative.
- Existing non-empty authoritative values remain protected.
- Conflict rows remain governed.
- Predictive candidate outputs remain non-production/uncalibrated.
- Scenario/simulation outputs remain separate from observed project facts.
- Government connector status is not changed.
- Reconciliation is read-only with respect to canonical project facts.
- Preflight is read-only.
- Health is read-only.

## New APIs

Administrator-only:

- `GET /api/admin/u75/bulk/health`
- `GET /api/admin/u75/bulk/reconciliation?ingestionRunId=<id>`
- `POST /api/admin/u75/bulk/preflight`

Safe promotion now also performs server-side downstream intelligence propagation for successfully promoted project IDs, so propagation does not depend on the browser completing a second request.

## UI

Data Integration now exposes:

- Bulk health
- Preflight
- Reconcile
- U75.9 downstream intelligence
- existing evidence ledger
- existing conflict review
- existing project/GIS drilldowns

## Verification performed in artifact environment

PASS:
- U73 language integrity — 34/34
- U72.4.2 functional — 9/9
- portfolio hygiene
- U74 final product — 19/19
- U75 bulk integration
- U75 dashboard drilldown
- U75.1 full integration
- U75.2–4 intelligence
- U75.5 evidence/promotion
- U75.6 command center
- U75.7 reliability
- U75.8 governed workbench
- U75.9 intelligence
- U75.10 closure — 17/17
- JSX syntax gate — 19 JSX/TSX files
- U75.10 pure-domain runtime
- ZIP integrity

A fresh dependency installation timed out in the artifact environment, so a fresh Vite production build of this exact U75.10 checkout was not claimed here. The Windows release gate below intentionally runs `npm.cmd run build` after installation and all regression smokes.


## Public review deployment closure

U75.10.1 adds an explicit public-review deployment profile without changing the protected operational workflow:

- public demo session is read-only Viewer scope;
- public demo can see only `synthetic_demo` projects;
- no public upload/edit/archive/promotion/admin access;
- external AI providers are blocked for public-demo sessions;
- public reviewers can submit rate-limited product feedback;
- feedback is stored in an additive `public_demo_feedback` table;
- Docker public-demo compose file and deployment runbook are included;
- a runtime smoke verifies the isolation boundary.

Use `PUBLIC_DEMO_DEPLOYMENT.md` for the public evaluation profile. The public profile must never be pointed at authoritative/private records.
